trait Identifiable {
  firstName;
  lastName;
  address;
  phoneNumber;
  fullName = {firstName + " " + lastName}
}

trait GeoLocatable{
  latitude;
  longitude;
}

trait EmailContactable{
  email;
}

trait Organization {
  Integer registrationNumber;
}

